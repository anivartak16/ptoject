import mongoose from 'mongoose'; import {Offer,Lot,Transaction,Payment,Notification} from '../models/index.js';
export async function acceptOffer(id,user){const session=await mongoose.startSession();try{let transaction;await session.withTransaction(async()=>{const offer=await Offer.findById(id).session(session);const lot=await Lot.findById(offer?.lot).session(session);if(!offer||!lot)throw Error('Offer not found');if(String(lot.owner)!==String(user._id))throw Error('Only lot owner can accept this offer');if(offer.status!=='PENDING')throw Error('Offer already processed');if(lot.remainingQuantity<offer.quantity)throw Error('Insufficient available quantity');lot.remainingQuantity-=offer.quantity;lot.status=lot.remainingQuantity===0?'SOLD':'PARTIALLY_SOLD';offer.status='ACCEPTED';transaction=await Transaction.create([{offer:offer._id,lot:lot._id,buyer:offer.buyer,seller:lot.owner,quantity:offer.quantity,amount:offer.totalAmount,status:'CREATED',events:[{status:'CREATED',note:'Offer accepted; transaction created'}]}],{session});await Payment.create([{transaction:transaction[0]._id,amount:offer.totalAmount}],{session});await Notification.create([{user:offer.buyer,message:'Your offer was accepted. Transaction created.',type:'OFFER_ACCEPTED'}],{session});await lot.save({session});await offer.save({session});transaction=transaction[0]});return transaction}finally{session.endSession()}}
